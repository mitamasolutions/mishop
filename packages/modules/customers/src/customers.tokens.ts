/** Tokens de inyección internos del módulo: puertos → adapters. */
export const CUSTOMERS_TOKENS = {
  customerRepository: 'customers.customer-repository',
  passwordHasher: 'customers.password-hasher',
} as const;
