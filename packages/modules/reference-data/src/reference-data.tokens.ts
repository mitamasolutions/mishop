/** Tokens de inyección internos del módulo: puertos → adapters. */
export const REFERENCE_DATA_TOKENS = {
  currencyRepository: 'reference-data.currency-repository',
  regionRepository: 'reference-data.region-repository',
  countryRepository: 'reference-data.country-repository',
} as const;
