/** Tokens de inyección internos del módulo: puertos → adapters. */
export const SETTINGS_TOKENS = {
  settingRepository: 'settings.setting-repository',
  settingsCache: 'settings.settings-cache',
} as const;
