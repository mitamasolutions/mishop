export const TAXES_TOKENS = {
  ruleRepository: 'taxes.rule-repository',
  storeSettingsRepository: 'taxes.store-settings-repository',
  providerRegistry: 'taxes.provider-registry',
  providers: 'taxes.providers',
} as const;
