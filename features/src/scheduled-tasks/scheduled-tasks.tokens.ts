/** Tokens de inyección internos del módulo (r24 · sprint1_cierre). */
export const SCHEDULED_TASKS_TOKENS = {
  repository: 'scheduled-tasks.repository',
  registry: 'scheduled-tasks.registry',
} as const;
