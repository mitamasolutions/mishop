/** Tokens de inyección internos del módulo: puertos → adapters. */
export const INVENTORY_TOKENS = {
  locationRepository: 'inventory.location-repository',
  itemRepository: 'inventory.item-repository',
} as const;
